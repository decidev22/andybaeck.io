import React from "react";
import styles from "../style";

const Project = () => {
  return (
    <section
      id="project"
      className={`flex items-start xs:flex-row flex-col ${styles.paddingY} min-h-screen`}
    >
      <div className="text-white">JogR_Software Projectssss</div>
    </section>
  );
};

export default Project;
